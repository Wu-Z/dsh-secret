# dsh-secret profile patch layer — insert one loader entry for this plugin.
# The package ships both halves: this host entry, and a browser half that
# `dsh.client.platform: web` hands to dsh-client-modules (see package.json).
- insert:
    - id: secret
      name: 'dsh-secret'
